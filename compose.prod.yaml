services:

  backend:
    build:
      context: ./server

    ports:
      - "3001:3001"

    env_file:
      - ./server/.env

  frontend:
    build:
      context: ./client

    ports:
      - "5173:5173"

    environment:
      VITE_API_URL: ${VITE_API_URL}

    depends_on:
      - backend

